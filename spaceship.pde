class Spaceship {
  // Position on screen.
  PVector _position;
  // Position on board.
  int _cellX, _cellY;
  // Display size.
  float _size;
      
  Spaceship() {
  }
  
  void move(Board board, PVector dir) {
  }
  
  void update(Board board) {
  }
  
  void drawIt() {
  }
}
